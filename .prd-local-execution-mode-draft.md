# 本地执行模式（Local Runtime）PRD 草案

## 背景

CoderFleet 今天所有账号都跑在独立 Docker 容器里,隔离靠容器边界 + `internal: true` 网络 + gost relay。本 PRD 要新增一种可选的 `local` runtime——账号不再起容器,直接调用宿主机上已安装的 `claude`/`codex` 二进制。依据是 `docs/research/local-execution-mode.md` 的调研结论(基于 Anthropic/OpenAI 官方文档、`openai/codex` 仓库源码、两个 CLI 的 `--help` 输出,以及在真实沙箱里对两个 CLI 的实测)。

全部改动拆成 7 个场景,场景 0 是无用户可见变化的前置清理,场景 1-6 按依赖顺序交付,每个场景可独立测试、独立验收。

---

## 场景 0:清理硬编码 `docker exec` 调用(前置依赖)

### 需求

`scheduler.py:3186-3195`(usage-status 检查)和 `scheduler.py:3234-3239`(`_get_hermes_session_id`)两处直接拼 `["docker","exec",...]` 并调用 `asyncio.create_subprocess_exec`,绕过了 `runtime.py:57` 定义的 `ContainerRuntime` Protocol。只要这两处还硬编码 docker,将来把某账号的 runtime 换成 `LocalRuntime` 后,这两条路径依然会对不存在的容器执行 `docker exec`,导致该账号的 usage-status 检查和 hermes session id 提取静默失败。这是所有后续本地执行工作的阻塞项。

### 方案

把这两处改写为调用 `self.runtime.exec(container_name, [...])`,与 `scheduler.py:2854/2894/2898/3021/3090` 等既有调用点用同一接口,调用方逻辑和返回值结构不变。

### 设计

- 不新增接口,`ContainerRuntime.exec` 签名(`runtime.py:57-72`)已经够用。
- 确认这两处调用方目前是否已经是 `Scheduler` 方法(能访问 `self.runtime`);若是独立函数,加一个 `runtime: ContainerRuntime` 形参。

### 实现方式

1. 定位 `scheduler.py:3186-3195` 与 `3234-3239` 的调用方(`_get_hermes_session_id` 等)。
2. 若为独立函数,加 `runtime` 形参,调用处传 `self.runtime`。
3. 替换硬编码 `asyncio.create_subprocess_exec("docker","exec",...)` 为 `await runtime.exec(container_name, argv)`。
4. 确认返回值解析逻辑适配 `runtime.exec` 的返回类型。

### 测试方法

- `tests/test_runtime.py` 用 `FakeRuntime.queue()` 给 hermes-session-id 场景和 usage-status 场景各构造一个 `FakeProcess`,断言调用经过了 `FakeRuntime.execs`。
- 现有覆盖这两个函数的测试跑一遍确认行为不变(不新增断言差异)。

### 验收标准

- [ ] `rtk grep -n "docker.*exec" coderfleet/server/scheduler.py` 结果里业务逻辑层不再直接拼 `"docker","exec"` 字符串(只剩 `runtime.py` 内部实现)。
- [ ] `FakeRuntime` 替换 `self.runtime` 后,hermes-session-id 和 usage-status 两条路径的单测通过,证明行为等价。
- [ ] `uv run --with pytest pytest -q` 全绿。

---

## 场景 1:`LocalRuntime` 执行后端

### 需求

让 Task 的实际执行(run/exec/存活检查/终止)能够选择"不经过 Docker、直接在宿主机跑子进程"这条路径,且复用现有调度器的状态机、日志采集、超时逻辑,不重写。

### 方案

新增 `LocalRuntime(ContainerRuntime)`,实现与 `DockerRuntime` 相同的 4 个方法,内部不拼 `docker run`/`docker exec` argv,直接发起本机子进程。

### 设计

- `run(spec)`:本地场景不需要镜像/挂载/网络这些 Docker 专属参数,直接 `asyncio.create_subprocess_exec(*spec.command, cwd=spec.workdir, env=merged_env, stdout=PIPE, stderr=STDOUT)`。
- `exec(container_name, argv)`:本地场景没有"容器名"概念,`container_name` 参数降级为"逻辑执行单元 ID"(可复用 account name 或 task id,仅用于日志追踪),`argv` 直接作为子进程命令执行。
- `is_running`:容器场景靠 `docker inspect`;本地场景靠 Task 记录里落的 `pid`,`os.kill(pid, 0)` 判断存活。
- `remove`:容器场景 `docker rm`;本地场景 `os.kill(pid, SIGTERM)`,超时后 `SIGKILL`。
- Scheduler 目前是全局单例 `self.runtime`(`scheduler.py:109`),改为按账号选择:新增 `self._runtime_for(acc: Account) -> ContainerRuntime`,`local` 返回 `LocalRuntime()` 单例、`container` 返回现有 `DockerRuntime()` 单例;所有原本直接用 `self.runtime` 的调用点改为 `self._runtime_for(acc)`。
- `ContainerSpec` 对 `acc.runtime == local` 的账号跳过 image/mounts/network 字段的构建,只保留 command/env/workdir。

### 实现方式

1. `runtime.py` 新增 `LocalRuntime` 类,实现 4 方法。
2. `models.py` Task 新增可选字段 `pid: int | None = None`(仅本地执行使用)。
3. `Scheduler.__init__` 保留 `DockerRuntime()` 默认值,新增 `self._local_runtime = LocalRuntime()`;所有 `self.runtime.xxx(...)` 调用点改为 `self._runtime_for(acc).xxx(...)`。
4. `ContainerSpec` 构建逻辑加 `acc.runtime == local` 分支短路。

### 测试方法

- 仿 `tests/test_runtime.py` 现有风格新增 `TestLocalRuntime`:用简单命令(如 `echo hello`)跑 `run`/`exec`,断言 stdout 正确、进程结束后 `is_running` 返回 `False`。
- "长时间运行命令 + `remove` 提前终止"用例,断言进程确实被杀(`os.kill(pid,0)` 抛 `ProcessLookupError`)。
- Scheduler 层新增"mixed fleet"用例:一个 local 账号 + 一个 container 账号同一 Scheduler 实例下,断言各自路由到正确的 runtime 实例。

### 验收标准

- [ ] `LocalRuntime` 单测覆盖 run/exec/is_running/remove 四方法,全部通过。
- [ ] 一个 local 账号任务从提交到完成的完整状态机(pending→running→completed)走通,日志正确采集到 `tasks/*.log`。
- [ ] 同一 Scheduler 实例下 local 账号和 container 账号互不干扰。
- [ ] `uv run --with pytest pytest -q` 全绿。

---

## 场景 2:`Account` 数据模型 + 凭证隔离

### 需求

需要一个字段标记账号是 container 还是 local;local 账号需要独立、互不冲突的 `CODEX_HOME`/`CLAUDE_CONFIG_DIR`,否则多个 local 账号会共用宿主机上同一份登录态,直接违反账号隔离这条北极星第一优先级。

### 方案

- `Account` 新增 `runtime: AccountRuntime = AccountRuntime.container` 字段(新枚举 `container|local`),默认值保证现有账号 100% 向后兼容。
- local 账号复用已存在的 `accounts/<name>/` 目录(今天已是容器挂载源,`scheduler.py:2759-2765`),直接当作该账号的 `CODEX_HOME`(`accounts/<name>/.codex`)和 `CLAUDE_CONFIG_DIR`(`accounts/<name>/.claude`),不新增存储位置。

### 设计

- `accounts.conf` 每行追加 `RUNTIME=container|local`(缺省按 container 处理,解析方式与现有 `PROXY` 字段一致,`models.py:141`)。
- 新增 `local_auth_dir(workspace_dir, acc) -> Path` helper,返回 `workspace_dir/accounts/<name>/.claude`(或 `.codex`),首次调用前 `mkdir -p`(Codex 在目录不存在时会硬失败,调研已确认)。
- 组装子进程 env(`LocalRuntime.run`/`exec`)时,把 `CODEX_HOME`/`CLAUDE_CONFIG_DIR` 设为上面解析出的宿主机绝对路径。

### 实现方式

1. `models.py` 新增 `AccountRuntime` 枚举、`Account.runtime` 字段、解析逻辑(仿 `AccountProxy`)、`AccountResponse` 同步带出。
2. `account_type_registry.py` 或 `scheduler.py` 新增 `local_auth_dir()`。
3. `coderfleet account add` CLI 新增 `--runtime local|container`,写入 `accounts.conf`。
4. `LocalRuntime` 组装 env 时调用该 helper。

### 测试方法

- `Account.from_conf_line` 解析带/不带 `RUNTIME=` 的两种行,断言默认值和显式值均正确。
- `local_auth_dir()` 纯函数单测:路径拼接正确、`mkdir -p` 幂等。
- 集成测试(需真实 CLI 环境,否则 skip):两个不同 account name 的 local 账号各自跑一次 `claude auth status`/`codex login status`,断言互不干扰。

### 验收标准

- [ ] 不写 `RUNTIME=` 的旧账号行解析后 `Account.runtime == container`,现有行为零变化。
- [ ] 新建 `RUNTIME=local` 账号,`local_auth_dir()` 路径在首次任务执行前被创建。
- [ ] 两个 local 账号并行跑任务,登录态/配置互不覆盖(至少手工验证一次)。
- [ ] `uv run --with pytest pytest -q` 全绿。

---

## 场景 3:宿主机 CLI 自动探测

### 需求

用户需要在选择 `--runtime local` 前确认"这台宿主机到底装没装 claude/codex、版本是什么",而不是盲选之后再报错。

### 方案

新增探测能力,复用 `AccountTypeSpec.login_cli`(`account_type_registry.py:1033`)作为唯一的"账号类型对应哪个二进制"事实来源,CLI 和 Web UI 共用同一个探测函数。

### 设计

- `detect_local_cli(spec: AccountTypeSpec) -> DetectResult`:`shutil.which(spec.login_cli)` 找路径,找到则跑 `<cli> --version`(按调研确认的格式解析,如 `2.1.220 (Claude Code)` / `codex-cli 0.146.0`),返回 `{found, path, version}`。
- 只回答"二进制存不存在、版本是什么",不做登录态检查(那是场景 2 的事)。

### 实现方式

1. 探测函数放在 `account_type_registry.py`(离 `login_cli` 数据源最近)。
2. CLI:新增 `coderfleet account detect`,遍历所有已注册 `AccountTypeSpec`,表格化输出;未找到时提示官方安装命令(curl 脚本/npm/brew,调研已收集)。
3. Web UI:新增 `GET /api/accounts/detect-local` endpoint,`main.py` 加路由;前端账号添加表单请求该接口,渲染检测结果作为 `--runtime local` 选项旁的提示。

### 测试方法

- `detect_local_cli` 单测:mock `shutil.which` 返回 `None`(未安装)和假路径 + mock 版本输出两种场景,断言解析正确。
- API 测试:mock 探测函数,断言 `/api/accounts/detect-local` 响应结构正确。
- CLI 输出快照测试:mock 探测函数,断言输出包含预期的"已安装/未安装"文案。

### 验收标准

- [ ] `coderfleet account detect` 在装有 claude/codex 的机器上正确报告版本;未装时给出安装提示而非崩溃。
- [ ] Web UI 添加账号表单能看到同样的探测结果(手工验证一次)。
- [ ] CLI 与 Web UI 复用同一个 `detect_local_cli` 函数,没有第二份探测逻辑。
- [ ] `uv run --with pytest pytest -q` 全绿。

---

## 场景 4:代理/网络隔离

### 需求

local 账号的出站流量默认没有 Docker `internal: true` 网络层兜底,必须明确回答"local + relay"这个组合支不支持,不能含糊上线让用户误以为和 container 账号一样安全。

### 方案(两个 CLI 不对称,调研已确认)

- Claude:官方文档确认标准代理环境变量对纯终端子进程调用生效,支持 `local + relay`。
- Codex:官方文档未确认代理支持,且上游有开放中的 issue(`openai/codex#4242`)证明这仍是未完成功能——**第一版直接禁止 `local + codex + relay` 组合**,只能选 `--proxy off`,错误信息说明原因并指向 `docs/research/local-execution-mode.md`。

### 设计

- 账号校验逻辑(`account add`/`apply` 两个入口共用同一函数)新增规则:`runtime==local and type==codex and proxy==relay` → 拒绝。
- Claude 的 `local+relay`:`LocalRuntime` 组装 env 时复用现有 `compose.py`/`scheduler.py` 的 `HTTPS_PROXY`/`ALL_PROXY`/`NO_PROXY` 注入逻辑(相同的 `RELAY_IP`/`RELAY_LISTEN_PORT` 读取方式,`scheduler.py:2668` 附近的 `_resolve_proxy_env` 若是私有方法需改造为可共享),只是写进 subprocess 的 `env=` 而不是 `docker run -e`。

### 实现方式

1. 校验函数放在 `models.py` 或 `cli.py` 的账号创建路径,`account add` 和 `apply` 两处都调用,避免跨入口重复实现漂移。
2. `LocalRuntime` env 组装对 Claude 账号复用 `_resolve_proxy_env`。
3. `docs/network.md` 补充"local runtime 的代理支持范围"说明。

### 测试方法

- 校验函数单测:`local+codex+relay` 断言拒绝并给出正确错误信息;`local+claude+relay` 断言通过。
- `LocalRuntime` env 组装单测:给定 relay 配置,断言 Claude 账号 subprocess env 里 `HTTPS_PROXY` 等变量值与 `RELAY_IP`/`RELAY_LISTEN_PORT` 一致。
- 手工验证:实际跑一个 `local+claude+relay` 任务,通过 relay 容器日志或流量观察确认真的经过了 relay(不能只看 env 变量设置就算通过)。

### 验收标准

- [ ] 创建 `local+codex+relay` 组合被明确拒绝,错误信息说明原因和依据来源。
- [ ] `local+claude+relay` 账号的实际任务流量经手工验证确实走了 relay。
- [ ] `docs/network.md` 更新,说明 local runtime 代理支持范围(Claude 支持、Codex 暂不支持,附上游 issue 链接)。
- [ ] `uv run --with pytest pytest -q` 全绿。

---

## 场景 5:沙箱/权限安全阀

### 需求

`auto=True` 任务今天直接用 `--dangerously-skip-permissions`(Claude)/`--dangerously-bypass-approvals-and-sandbox`(Codex),两者官方文档都明确"仅推荐已被外部沙箱隔离的环境使用"。local 账号没有容器兜底,继续无脑传这两个旗标是真实的安全回退,必须在上线前堵住。

### 方案

local 账号 + `auto=True` 时禁止使用 dangerous 旗标;要求账号已显式确认开启 CLI 自带的 OS 级沙箱(Claude `sandbox.enabled=true`+`sandbox.failIfUnavailable=true`;Codex `--sandbox workspace-write`),未确认则调度器直接拒绝任务,不静默降级。

### 设计

- 由 CoderFleet 自己维护一个"账号已确认开启沙箱"标记 `Account.sandbox_confirmed: bool = False`,由用户在 `account add`/`account edit` 时显式设置——不代为解析对方 CLI 的配置文件(更简单、不依赖对方配置格式稳定性,责任显式交给操作者)。
- `_build_claude`/`_build_codex`(`account_type_registry.py:224-257`)的 `auto=True` 分支:`if runtime==local and auto and not sandbox_confirmed: raise`(在调度器提交任务阶段拦截,不进入 CLI 拼装);`local+sandbox_confirmed` 时 Codex 换成 `--sandbox workspace-write`(放弃 danger-full-access/bypass),Claude 仍用 `--dangerously-skip-permissions`(该旗标只跳过审批 UI,真正隔离靠用户已手工开启的 `sandbox.enabled`,两者不冲突)。

### 实现方式

1. `Account` 新增 `sandbox_confirmed: bool = False`(仅 `runtime=local` 有意义)。
2. `coderfleet account add --runtime local` 搭配 `--auto` 使用时,要求显式 `--sandbox-confirmed` 标志,写入账号记录。
3. Scheduler 提交任务入口新增前置校验。
4. `_build_codex` 区分 local+auto+sandbox_confirmed 场景,参数换成 `--sandbox workspace-write`。

### 测试方法

- 校验逻辑单测:`local+auto=True+sandbox_confirmed=False` 断言拒绝;`sandbox_confirmed=True` 断言通过。
- `_build_codex`/`_build_claude` 参数拼装单测:local+confirmed 场景断言不再出现 bypass 旗标,改为 `--sandbox workspace-write`。
- container 账号回归测试:确认所有既有 `_build_claude`/`_build_codex` 用例输出不变。

### 验收标准

- [ ] 未设置 `sandbox_confirmed` 的 local 账号提交 `auto=True` 任务被拒绝,错误信息清晰。
- [ ] `sandbox_confirmed=True` 的 local Codex 账号实际拼出 `--sandbox workspace-write` 而非 bypass 旗标。
- [ ] container 账号行为零回归。
- [ ] `uv run --with pytest pytest -q` 全绿。

---

## 场景 6:CLI / Web UI 对等 + 文档收尾

### 需求

按北极星第二优先级,新能力不能只在一端可用;按"编辑 load-bearing 文件后重写文档条目"规则,这次改动涉及的核心文件要同步更新文档。

### 方案

- `coderfleet account add <name> TYPE=... --runtime local|container [--sandbox-confirmed]`:CLI 侧完整闭环。
- Web UI 账号添加/编辑表单加对应字段(runtime 单选 + sandbox 确认勾选框,仅 local 时可见),复用场景 3 的探测结果做提示。
- 文档:`docs/agents/key-files.md` 更新 `runtime.py`/`scheduler.py`/`account_type_registry.py`/`models.py` 条目;`docs/accounts.md`/`docs/network.md` 补充 local runtime 说明;`CLAUDE.md` 的"网络隔离"章节补一句说明 local 是有限隔离模式。

### 设计 / 实现方式

1. `cli.py` 的 `account add` 命令新增两个 option,透传给 `Account` 构造。
2. Web UI:账号表单文件新增字段,对应的账号创建 endpoint 接收新字段。
3. 文档按上述文件逐一更新(重写条目,不追加历史记录)。

### 测试方法

- CLI 端到端测试:`coderfleet account add test-local TYPE=claude --runtime local`,断言 `accounts.conf` 出现正确的 `RUNTIME=local` 行。
- Web UI 手工验证:浏览器里走一遍添加 local 账号完整流程(探测提示→勾选沙箱确认→提交成功)。
- 文档 review:确认场景 0-5 涉及文件在 `docs/agents/key-files.md` 的条目已反映新行为。

### 验收标准

- [ ] CLI 和 Web UI 都能完整创建可用的 local 账号,两端字段一致、行为一致。
- [ ] `docs/agents/key-files.md`、`docs/accounts.md`、`docs/network.md`、`CLAUDE.md` 均已更新为反映当前行为。
- [ ] `uv run --with pytest pytest -q` 全绿,`bash scripts/verify.sh` 通过。

---

## Out of Scope(全部场景通用)

- 不做 Codex 的宿主机级网络强制隔离(独立 OS 用户 + iptables/nftables)——记录为"待上游 issue #4242 关闭或有明确需求再评估"的后续项,不在本 PRD 范围。
- 不代为解析/写入 Claude、Codex 各自的 CLI 配置文件(`settings.json`/`config.toml`)——沙箱确认由用户显式声明,CoderFleet 不做"帮用户改对方配置"这类隐性操作。
- 不为 local 账号新增专属的容器生命周期管理 UI(启动/停止/日志的 Web UI 展示复用现有 Task 日志机制,不单独设计)。
- 不改变 `Board`/`WorkflowRun` 对 runtime 类型的感知——它们仍然只关心 Conversation/WorkflowRun,不下钻到账号的 runtime 细节。

## Further Notes

- 完整调研见 `docs/research/local-execution-mode.md`(基于 Anthropic/OpenAI 官方文档 + `openai/codex` 仓库源码 + 两个 CLI 的 `--help` 输出与实测)。
- 推荐落地顺序:场景 0+1+2+3 合并为第一个 PR(骨架+探测,`runtime` 默认值保证向后兼容,零风险);场景 4+5 单独一个 PR(真正的安全/隔离决策,建议先只放开 Claude 的 `local+relay` 全量支持);场景 6 跟功能一起进各自的 PR。
